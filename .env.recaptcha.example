# Frontend — Google reCAPTCHA v2 site key (public)
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=

# Backend — Google reCAPTCHA v2 secret key (private)
# Add the same pair in flyoci-backend/.env:
# RECAPTCHA_SECRET_KEY=
# RECAPTCHA_SITE_KEY=
